services:

  # ── OTel Collector ───────────────────────────────────────────────────────────
  # Receives all signals from generators and forwards to Cribl Stream.
  # zpages debug UI: http://localhost:55679/debug/tracez

  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    container_name: otel-collector
    command: ["--config=/etc/otel/config.yaml"]
    environment:
      - CRIBL_ENDPOINT=${CRIBL_ENDPOINT}
    volumes:
      - ./otel-config.yaml:/etc/otel/config.yaml
    ports:
      - "4317:4317"   # OTLP gRPC (receives from all generators)
      - "55679:55679" # zpages debug UI


  # ── Traces ───────────────────────────────────────────────────────────────────

  telemetrygen-traces-frontend:
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    container_name: telemetrygen-traces-frontend
    depends_on:
      - otel-collector
    restart: on-failure
    command:
      - traces
      - --otlp-endpoint=otel-collector:4317
      - --otlp-insecure
      - --rate=20
      - --workers=1
      - --duration=inf
      - --service=workshop-frontend
      - --child-spans=4
      - --otlp-attributes=deployment.environment="production"
      - --otlp-attributes=service.version="2.4.1"
      - --telemetry-attributes=http.method="GET"
      - --telemetry-attributes=http.route="/checkout"
      - --telemetry-attributes=http.status_code="200"
      - --telemetry-attributes=region="us-east-1"

  telemetrygen-traces-checkout:
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    container_name: telemetrygen-traces-checkout
    depends_on:
      - otel-collector
    restart: on-failure
    command:
      - traces
      - --otlp-endpoint=otel-collector:4317
      - --otlp-insecure
      - --rate=10
      - --workers=1
      - --duration=inf
      - --service=workshop-checkout
      - --child-spans=4
      - --otlp-attributes=deployment.environment="production"
      - --otlp-attributes=service.version="1.7.0"
      - --telemetry-attributes=customer.tier="premium"
      - --telemetry-attributes=payment.method="card"
      - --telemetry-attributes=cart.item_count="3"
      - --telemetry-attributes=region="us-east-1"

  telemetrygen-traces-payment:
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    container_name: telemetrygen-traces-payment
    depends_on:
      - otel-collector
    restart: on-failure
    command:
      - traces
      - --otlp-endpoint=otel-collector:4317
      - --otlp-insecure
      - --rate=3
      - --workers=1
      - --duration=inf
      - --service=workshop-payment
      - --child-spans=4
      - --otlp-attributes=deployment.environment="production"
      - --otlp-attributes=service.version="3.1.2"
      - --telemetry-attributes=processor.name="stripe"
      - --telemetry-attributes=card.type="visa"
      - --telemetry-attributes=transaction.currency="USD"
      - --telemetry-attributes=region="us-east-1"


  # ── Logs ─────────────────────────────────────────────────────────────────────
  # Replaces the basic telemetrygen-logs-info / telemetrygen-logs-error services.
  # Generates realistic multi-scenario logs including PII (phone numbers, card
  # numbers in bodies) for the PII redaction exercise, LLM operation logs,
  # HTTP access logs, payment events, and session lifecycle events.
  # Source in ./workshop-log-gen/ — see generate.py for the full scenario schema.

  workshop-logs:
    build: ./workshop-log-gen
    container_name: workshop-log-gen
    depends_on:
      - otel-collector
    restart: on-failure
    environment:
      # Send to the local OTel collector — it forwards to Cribl
      OTEL_EXPORTER_OTLP_ENDPOINT: otel-collector:4317
      # No TLS on the local collector-to-generator path
      OTEL_INSECURE: "true"
      # Events per second across all scenario types
      LOG_RATE_EPS: "3"
      # Uncomment to pin to a single service name:
      # SERVICE_NAME: workshop-ai-assistant


  # ── GenAI / LLM Traces ───────────────────────────────────────────────────────
  # Synthetic LLM telemetry generator. Emits OTel GenAI-shaped spans with
  # realistic prompt/completion bodies, token counts, per-call cost, model
  # mix, and a small percentage of errors and slow-tail latencies.
  # Powers the workshop's AI-observability demos: PII masking on prompts,
  # cost-per-route aggregation, prompt/completion archival to Lake, and
  # outcome-aware tail sampling.
  # Source in ./genaitelgen/ — see main.py for the full attribute schema.

  genaitelgen:
    build: ./genaitelgen
    container_name: genaitelgen
    depends_on:
      - otel-collector
    restart: on-failure
    environment:
      OTLP_ENDPOINT: otel-collector:4317
      SERVICE_NAME: workshop-ai-assistant
      SERVICE_VERSION: 1.0.0
      DEPLOYMENT_ENV: production
      RATE_PER_SEC: "2"
